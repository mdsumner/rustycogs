% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/rustycogs.R
\name{tiff_tile}
\alias{tiff_tile}
\title{Fetch and decode a single tile from a TIFF/COG file.}
\usage{
tiff_tile(path, ifd_index = 0L, col, row, region = NULL, anon = FALSE)

tiff_tile(path, ifd_index = 0L, col, row, region = NULL, anon = FALSE)
}
\arguments{
\item{path}{File path or URL to the TIFF.}

\item{ifd_index}{IFD index (0-based). \code{0} is the full-resolution image.}

\item{col}{Tile column index (0-based).}

\item{row}{Tile row index (0-based).}

\item{region}{Optional AWS region string.}

\item{anon}{Logical. Use anonymous requests. Default \code{FALSE}.}
}
\value{
A named list with data, dim, dtype.

A named list with:
\itemize{
\item \code{data}: numeric vector of decoded pixel values (row-major)
\item \code{dim}: integer vector \code{c(height, width, bands)}
\item \code{dtype}: character dtype string (e.g. \code{"<f4"}, \code{"<u2"})
}
}
\description{
Fetch and decode a single tile from a TIFF/COG file.

Fetch and decode a single tile from a TIFF/COG file
}
\examples{
\dontrun{
tile <- tiff_tile("scene.tif", col = 0L, row = 0L)
m <- tile_to_array(tile)
}
}
\seealso{
\code{\link[=tile_to_array]{tile_to_array()}} to reshape the result, \code{\link[=tiff_tiles]{tiff_tiles()}} for batches
}
