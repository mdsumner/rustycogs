% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/rustycogs.R
\name{tiff_ifd_info}
\alias{tiff_ifd_info}
\title{Summarise IFD-level metadata from TIFF/COG files.}
\usage{
tiff_ifd_info(paths, region = NULL, anon = FALSE, concurrency = 16L)

tiff_ifd_info(paths, region = NULL, anon = FALSE, concurrency = 16L)
}
\arguments{
\item{paths}{Character vector of file paths or URLs.}

\item{region}{Optional AWS region string (e.g. \code{"ap-southeast-2"}).}

\item{anon}{Logical. Use anonymous/unsigned requests. Default \code{FALSE}.}

\item{concurrency}{Integer. Maximum concurrent file scans. Default \code{16L}.}
}
\value{
A data.frame with one row per IFD containing image dimensions,
tile grid size, dtype, compression, photometric interpretation,
predictor, planar configuration, CRS, nodata, and geotransform.

A data frame with one row per IFD and columns: \code{path}, \code{ifd},
\code{is_tiled}, \code{image_w}, \code{image_h}, \code{tile_w}, \code{tile_h}, \code{n_tiles_x},
\code{n_tiles_y}, \code{dtype}, \code{compression}, \code{bits_per_sample},
\code{samples_per_pixel}, \code{photometric}, \code{predictor},
\code{planar_configuration}, \code{crs_epsg}, \code{gdal_nodata}, \code{scale_x},
\code{scale_y}, \code{origin_x}, \code{origin_y}.
}
\description{
Returns one row per IFD per file — no tile explosion. Useful for
understanding file structure before deciding which tiles to fetch.

Returns one row per IFD per file with no tile explosion. Use this to
understand file structure — how many IFDs (overviews), tile grid dimensions,
dtype, CRS, geotransform — before deciding which tiles to fetch with
\code{\link[=tiff_refs]{tiff_refs()}} or \code{\link[=tiff_read_tiles]{tiff_read_tiles()}}.
}
\examples{
\dontrun{
tiff_ifd_info("scene.tif")
tiff_ifd_info("s3://my-bucket/data.tif", anon = TRUE)
}
}
\seealso{
\code{\link[=tiff_refs]{tiff_refs()}} for the full per-tile data frame
}
