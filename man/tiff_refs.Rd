% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/rustycogs.R
\name{tiff_refs}
\alias{tiff_refs}
\title{Extract tile byte-range references from TIFF/COG files.}
\usage{
tiff_refs(paths, region = NULL, anon = FALSE, concurrency = 16L)

tiff_refs(paths, region = NULL, anon = FALSE, concurrency = 16L)
}
\arguments{
\item{paths}{Character vector of file paths or URLs.
Supported schemes: \verb{s3://}, \verb{gs://}, \verb{az://}, \verb{http://}, \verb{https://},
or plain local paths.}

\item{region}{Optional AWS region string (e.g. \code{"ap-southeast-2"}).
Ignored for non-S3 URLs.}

\item{anon}{Logical. Use anonymous/unsigned requests. Default \code{FALSE}.}

\item{concurrency}{Integer. Maximum number of files scanned concurrently.
Default \code{16L}.}
}
\value{
A data.frame with columns path, ifd, tile_col, tile_row,
offset, length, image_w, image_h, tile_w, tile_h, valid_w, valid_h,
dtype, compression, bits_per_sample, samples_per_pixel, crs_epsg,
gdal_nodata, planar_configuration, scale_x, scale_y,
origin_x, origin_y.

A data frame with columns: \code{path}, \code{ifd}, \code{tile_col}, \code{tile_row},
\code{offset}, \code{length}, \code{image_w}, \code{image_h}, \code{tile_w}, \code{tile_h}, \code{dtype},
\code{compression}, \code{bits_per_sample}, \code{samples_per_pixel}, \code{crs_epsg},
\code{gdal_nodata}, \code{planar_configuration}, \code{scale_x}, \code{scale_y},
\code{origin_x}, \code{origin_y}.
}
\description{
Scans one or more TIFF or COG files and returns a data frame with one row
per tile per IFD, containing the byte-range metadata needed to construct
Kerchunk or Zarr virtual stores.
}
\examples{
\dontrun{
refs <- tiff_refs("s3://my-bucket/data.tif", anon = TRUE)
refs <- tiff_refs(c("file1.tif", "file2.tif"), concurrency = 4L)
}
}
\seealso{
\code{\link[=tiff_ifd_info]{tiff_ifd_info()}} for a compact one-row-per-IFD summary,
\code{\link[=tiff_read_tiles]{tiff_read_tiles()}} to fetch pixel data
}
