% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/rustycogs.R
\name{tiff_tiles}
\alias{tiff_tiles}
\title{Fetch and decode multiple tiles from a TIFF/COG file.}
\usage{
tiff_tiles(path, ifd_index = 0L, cols, rows, region = NULL, anon = FALSE)

tiff_tiles(path, ifd_index = 0L, cols, rows, region = NULL, anon = FALSE)
}
\arguments{
\item{path}{File path or URL to the TIFF.}

\item{ifd_index}{IFD index (0-based). Default \code{0L}.}

\item{cols}{Integer vector of tile column indices (0-based).}

\item{rows}{Integer vector of tile row indices (0-based). Must be the same
length as \code{cols}.}

\item{region}{Optional AWS region string.}

\item{anon}{Logical. Use anonymous requests. Default \code{FALSE}.}
}
\value{
A list of tile result lists, each with data, dim, dtype.

A list of tile results, each with \code{data}, \code{dim}, and \code{dtype}
components as described in \code{\link[=tiff_tile]{tiff_tile()}}.
}
\description{
Uses ifd.fetch_tiles() for a single vectorized range request.

Fetches a batch of tiles from a single file in one vectorized request.
More efficient than calling \code{\link[=tiff_tile]{tiff_tile()}} in a loop. For multi-file
batches use \code{\link[=tiff_read_tiles]{tiff_read_tiles()}}.
}
\examples{
\dontrun{
tiles <- tiff_tiles("scene.tif", cols = 0:3, rows = rep(0L, 4))
arrays <- lapply(tiles, tile_to_array)
}
}
\seealso{
\code{\link[=tile_to_array]{tile_to_array()}} to reshape individual results,
\code{\link[=tiff_read_tiles]{tiff_read_tiles()}} for multi-file batches
}
