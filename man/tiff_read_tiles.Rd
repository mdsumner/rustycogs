% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/rustycogs.R
\name{tiff_read_tiles}
\alias{tiff_read_tiles}
\title{Internal entry point for tiff_read_tiles().}
\usage{
tiff_read_tiles(refs, region = NULL, anon = FALSE, concurrency = 16L)

tiff_read_tiles(refs, region = NULL, anon = FALSE, concurrency = 16L)
}
\arguments{
\item{refs}{A data frame from \code{\link[=tiff_refs]{tiff_refs()}}, or a subset of one.}

\item{region}{Optional AWS region string. Default \code{NULL}.}

\item{anon}{Logical. Use anonymous requests. Default \code{FALSE}.}

\item{concurrency}{Integer. Max concurrent files. Default \code{16L}.}
}
\value{
\code{refs} with a \code{data} list-column appended.
}
\description{
The public R API is the \code{\link[=tiff_read_tiles]{tiff_read_tiles()}} wrapper in \code{R/rustycogs.R}
which accepts a refs data frame and returns it with a \code{data} list-column.
This function takes the raw column vectors directly.

Takes a subset of the data frame from \code{\link[=tiff_refs]{tiff_refs()}} and returns it
with a \code{data} list-column added. Each element of \code{data} is a numeric
vector of decoded pixel values in row-major order, suitable for
passing to \code{\link[=tile_to_array]{tile_to_array()}}.
}
\details{
Files are opened once each and tiles fetched in a single vectorized
range request per file. Row order of the input is preserved.
}
\examples{
\dontrun{
refs <- tiff_refs("scene.tif")
refs <- tiff_read_tiles(refs)
arrays <- lapply(refs$data, tile_to_array)
}
}
\seealso{
\code{\link[=tiff_refs]{tiff_refs()}}, \code{\link[=tile_to_array]{tile_to_array()}}
}
\keyword{internal}
