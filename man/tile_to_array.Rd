% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rustycogs.R
\name{tile_to_array}
\alias{tile_to_array}
\title{Convert a tile result to a matrix or array}
\usage{
tile_to_array(tile)
}
\arguments{
\item{tile}{A tile result list from \code{\link[=tiff_tile]{tiff_tile()}}, \code{\link[=tiff_tiles]{tiff_tiles()}}, or
an element of the \code{data} column from \code{\link[=tiff_read_tiles]{tiff_read_tiles()}}.}
}
\value{
A matrix for single-band tiles, or a 3D array with dimensions
\verb{[height, width, bands]} for multi-band tiles.
}
\description{
Reshapes the flat numeric vector returned by \code{\link[=tiff_tile]{tiff_tile()}}, each element
of \code{\link[=tiff_tiles]{tiff_tiles()}}, or each element of the \code{data} column from
\code{\link[=tiff_read_tiles]{tiff_read_tiles()}} into a matrix (single band) or 3D array (multi-band).
}
\details{
The matrix is filled row-by-row (\code{byrow = TRUE}), consistent with the
row-major (C) order returned by async-tiff and expected by
\code{\link[graphics:rasterImage]{graphics::rasterImage()}} and \code{ximage()}. Note that \code{as.vector(m)} on the
result does \strong{not} recover the original input order; use \code{as.vector(t(m))}
for a round-trip.
}
\examples{
\dontrun{
tile <- tiff_tile("scene.tif", col = 0L, row = 0L)
m <- tile_to_array(tile)
dim(m)  # c(256, 256) for a single-band 256x256 tile
ximage::ximage(m)
}
}
